# tmux-layout.plugin.zsh — zsh plugin entry point
#
# Adds the tmux-layout binary to PATH so it is invocable as a command.
# Compatible with zinit, oh-my-zsh, antigen, zplug, and manual sourcing.

export PATH="${0:A:h}:$PATH"
